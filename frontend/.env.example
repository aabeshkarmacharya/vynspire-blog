# Next.js frontend environment

# Public base URL used by the browser for API requests
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Internal base URL used by server-side rendering (SSR) inside Docker network
INTERNAL_API_BASE=http://backend:8000

# Disable Next telemetry in development
NEXT_TELEMETRY_DISABLED=1

# Usage:
#   cp frontend/.env.example frontend/.env
# Next.js loads .env automatically in development.
